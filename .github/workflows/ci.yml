# Legacy/reference repository CI.
# Run this workflow manually when intentionally checking the old Docker stack.

name: CI

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Build Docker containers
        run: docker compose build